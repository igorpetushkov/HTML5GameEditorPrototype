import tabs from './tabs';

import objects from './objects';
import scenes from './scenes';
import layers from './layers';
import tags from './tags';

export default { world: {
    tabs, 
    objects, scenes, layers, tags, 
}};